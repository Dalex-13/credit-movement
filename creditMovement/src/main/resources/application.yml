
server:
  port: 8084 #${PORT:0}
spring:
  application:
    name: service-CreditMovement
  main:
    allow-bean-definition-overriding: true
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: CreditMovement

eureka:
  client:
  service-url:
  defaultZone: http://localhost:8761/eureka